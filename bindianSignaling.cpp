#include <iostream>

using namespace std;

int main(){
#ifndef ONLINE_JUDGE
    freopen("main.inp", "r", stdin);
#endif
    int numHill, *hillHeights;
    cin >> numHill;
    hillHeights = new int[numHill];
    for(int i = 0 ; i < numHill ; ++ i) cin >> hillHeights[i];
    
}